**Use uci config to create .jsonnet files**

To do it the "OpenWRT way" we should try to make a uci config to reaction config script inside our init.d file. So we could get use of the uci system and for example creating a luci app would be much easier.
